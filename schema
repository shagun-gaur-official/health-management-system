-- db/schema.sql
CREATE DATABASE IF NOT EXISTS healthdb;
USE healthdb;
CREATE TABLE IF NOT EXISTS patients (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  age INT,
  gender VARCHAR(10),
  contact VARCHAR(50),
  medical_history TEXT
);

INSERT INTO patients (name, age, gender, contact, medical_history) VALUES
('Asha Singh', 34, 'Female', '9876543210', 'No major history'),
('Ramesh Kumar', 45, 'Male', '9123456780', 'Diabetic');
